machine:
  services:
    - docker
checkout:
  post:
    - git clone https://github.com/somaticio/somaticagent.git


dependencies:
  override:
    - echo "skipping pip install" 

test:
  override:
    - tests/test_api.sh
