checkout:
  post:
    - git clone https://github.com/somaticio/somaticagent.git

dependencies:
  post:
    - pip install -r somaticagent/requirements.txt

test:
  override:
    - python somaticagent/apitester.py -n 5